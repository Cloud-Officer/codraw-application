# Application

Symfony bundle to handle application domain logic: configuration registry, feature toggles, system monitoring and application versioning.

## Installation

```bash
composer require codraw/
```
